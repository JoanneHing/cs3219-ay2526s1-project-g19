from django.urls import path

urlpatterns = [
    # Placeholder for user profile endpoints
    # path('me/', UserMeView.as_view(), name='user-me'),
    # path('profile/', UserProfileUpdateView.as_view(), name='user-profile-update'),
]