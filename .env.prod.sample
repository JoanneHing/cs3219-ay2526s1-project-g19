# =============================================================================
# PeerPrep Microservices - Production Environment
# =============================================================================
# WARNING: NEVER COMMIT THIS FILE WITH REAL SECRETS
# Copy this file to .env.prod and fill in actual production values
# =============================================================================

ENVIRONMENT=production

# -----------------------------------------------------------------------------
# Production Secrets (NEVER COMMIT THESE)
# -----------------------------------------------------------------------------
PROD_SECRET_KEY=your-super-secret-production-key-min-50-chars
PROD_ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com,www.yourdomain.com

# -----------------------------------------------------------------------------
# Production Database URLs (Managed Services)
# -----------------------------------------------------------------------------
# Replace with your actual managed database URLs (AWS RDS, Cloud SQL, etc.)
PROD_QUESTION_DATABASE_URL=postgresql://user:password@prod-db-host:5432/question_db
PROD_MATCHING_DATABASE_URL=postgresql://user:password@prod-db-host:5432/matching_db
PROD_HISTORY_DATABASE_URL=postgresql://user:password@prod-db-host:5432/history_db
PROD_USER_DATABASE_URL=postgresql://user:password@prod-db-host:5432/user_db

# -----------------------------------------------------------------------------
# Production Redis URLs (Managed Services)
# -----------------------------------------------------------------------------
# Option A: Single managed Redis with different databases
PROD_MATCHING_REDIS_URL=redis://:password@prod-redis-host:6379/0
PROD_COLLABORATION_REDIS_URL=redis://:password@prod-redis-host:6379/1
PROD_CHAT_REDIS_URL=redis://:password@prod-redis-host:6379/2

# Option B: Separate managed Redis instances (uncomment if using this approach)
# PROD_MATCHING_REDIS_URL=redis://:password@matching-redis-host:6379/0
# PROD_COLLABORATION_REDIS_URL=redis://:password@collab-redis-host:6379/0
# PROD_CHAT_REDIS_URL=redis://:password@chat-redis-host:6379/0

# -----------------------------------------------------------------------------
# Service URLs (Production)
# -----------------------------------------------------------------------------
FRONTEND_URL=https://yourdomain.com
API_GATEWAY_URL=https://api.yourdomain.com

# -----------------------------------------------------------------------------
# Service Ports (Production - usually behind load balancer)
# -----------------------------------------------------------------------------
FRONTEND_PORT=5173
QUESTION_SERVICE_PORT=8001
MATCHING_SERVICE_PORT=8002
HISTORY_SERVICE_PORT=8003
USER_SERVICE_PORT=8004
COLLABORATION_SERVICE_PORT=8005
CHAT_SERVICE_PORT=8006
